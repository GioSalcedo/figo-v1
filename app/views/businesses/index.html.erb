<h1>Businesses#index</h1>
<p>Find me in app/views/businesses/index.html.erb</p>

<%= link_to "New Empresa", new_user_business_path %>

  <% @businesses.each do |business| %>
    <div class="card">
      <div class="card-body">
        <%= business.name %>
        <%= business.description %>
        <%= business.logo %>
      </div>
    </div>
  <% end %>
