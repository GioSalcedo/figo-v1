<h1>Businesses#show</h1>
    <div class="card">
      <div class="card-body">
        <h5><%= @business.name %></h5>
        <p><%= @business.description %></p>
        <p><%= @business.logo %></p>
      </div>
    </div>
<%= link_to "Regresar", businesses_path %>
<%= link_to "Editar", edit_business_path %>
<%= button_to "Eliminar", business_path, method: :delete, data: { confirm: "Are you sure?" }, class:"btn" %>
