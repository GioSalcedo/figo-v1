<h1>Metas</h1>
          <p class="card-text"><%= link_to "Agregar Nueva Meta", new_saving_path, class:"btn btn-secundary mb-2" %> </p>
<div class="row">
  <% @savings.each do |saving| %>
    <div class="col-lg-3 col-md-6 d-flex my-3">
      <div class="card">
        <div class="card-body">
        <%= link_to saving.name, saving %>
        <p class="card-text">En esta tarjeta encuentras la información del <%= saving.name %>, si necesitas saber más, puedes hacer click aquí.</p>
        </div>
      </div>
    </div>
   <% end %>
        </div>
      </div>
    </div>
</div>
